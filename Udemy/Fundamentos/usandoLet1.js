let numero = 1
    {
        let numero = 2
        console.log('dentro =', numero)
    }
console.log('fora =', numero)

//variáveis em VAR. tem escopo global e de função.
// o LET, tem escopo global, função e de bloco, e isso faz diferença em várias questões problematicas do tipo VAR.